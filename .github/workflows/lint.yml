name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: yarn
    with:
      files: src/
    - name: Run ESLint
      run: eslint . --ext .js,.svelte